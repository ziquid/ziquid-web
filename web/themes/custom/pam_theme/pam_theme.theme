<?php

/**
 * @file
 * Functions to support theming in the Pam theme.
 */

/**
 * Implements hook_preprocess_HOOK() for HTML document templates.
 */
function pam_theme_preprocess_html(&$variables) {

  // Add pam class to the body.
  $variables['attributes']['class'][] = 'pam';
}
